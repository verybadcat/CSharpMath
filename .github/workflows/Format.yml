name: Check format

on: [push, pull_request]
jobs:
  Excluding_Typography:
    if: (github.event_name == 'pull_request') == github.event.pull_request.head.repo.fork
    runs-on: windows-latest
    steps:
    - uses: actions/setup-dotnet@main
      if: matrix.os != 'ubuntu-latest'
      with:
        dotnet-version: '10.x'
        workloads: maui-ios, maui-tizen, maui-android, maui-maccatalyst, wasm-tools
    - uses: actions/setup-dotnet@main
      if: matrix.os == 'ubuntu-latest'
      with:
        dotnet-version: '10.x'
        workloads: maui-tizen, maui-android, maui-maccatalyst, wasm-tools
    - uses: actions/checkout@v6
      with:
        submodules: 'recursive'
    - name: Check formatting (Fix with "dotnet format --exclude Typography" at repository root)
      run: dotnet format --exclude Typography --verify-no-changes --severity error --verbosity diagnostic